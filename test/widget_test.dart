import 'package:flutter_test/flutter_test.dart';

/// Phase 1 smoke test — verifies the test infrastructure is working.
/// Full widget tests will be added in later phases when screens are complete.
void main() {
  test('placeholder test — passes by definition', () {
    expect(1 + 1, 2);
  });
}
